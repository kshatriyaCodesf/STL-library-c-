#include<iostream>
#include<map>
using namespace std;
int main(){
    map<string , string>m;
    m["abcd"] = "abcd"; // toc of the insertion is not always  O(log n) the insertion time also depend upon the key real time  complexity is : s.size()*log(n)
    
    
return 0;
}